import os

# Generate a 32-byte (256-bit) key
encryption_key = os.urandom(32)

# Generate a 16-byte (128-bit) IV
encryption_iv = os.urandom(16)

print("Key (hex):", encryption_key.hex())
print("IV (hex):", encryption_iv.hex())